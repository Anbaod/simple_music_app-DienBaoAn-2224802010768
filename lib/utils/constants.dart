import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF191414);

  static const Color surface = Color(0xFF282828);

  static const Color primary = Color(0xFF1DB954);

  static const Color textPrimary = Colors.white;

  static const Color textSecondary = Colors.grey;

  static const Color error = Colors.red;

  static const Color divider = Color(0xFF404040);
}

class AppSizes {
  static const double padding = 16;

  static const double radius = 12;

  static const double iconSize = 24;

  static const double miniPlayerHeight = 70;

  static const double albumArtSize = 300;
}

class AppStrings {
  static const String appName = 'Simple Music App';

  static const String defaultArtist = 'Unknown Artist';

  static const String defaultAlbum = 'Unknown Album';
}